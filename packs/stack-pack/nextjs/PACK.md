# Stack Pack: nextjs

```yaml
pack_metadata:
  pack_id: "stack.nextjs"
  version: "0.1.0"
  scenario: "coding"
  risk_level: "medium"
  expected_gain: "降低渲染边界错误与构建失败"
  validation_suite: "intent-benchmark-coding-v1"
```

Focus: SSR/SSG boundaries, route rendering behavior, build stability.

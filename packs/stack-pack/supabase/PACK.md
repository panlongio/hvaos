# Stack Pack: supabase

```yaml
pack_metadata:
  pack_id: "stack.supabase"
  version: "0.1.0"
  scenario: "coding"
  risk_level: "high"
  expected_gain: "降低 SQL 误用与策略偏差风险"
  validation_suite: "intent-benchmark-coding-v1"
```

Focus: SQL safety, migration discipline, RLS consistency.

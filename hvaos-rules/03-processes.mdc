---
description: Process card aligned with 03-processes.md.
globs: *
alwaysApply: true
---
# 03 - 流程卡片 (Processes)

默认执行最小闭环：
1. 意图确认
2. 方案提交
3. 审批执行
4. 验证交付

复杂任务执行完整闭环：
1. 需求分析
2. 技术方案
3. 人类审批
4. 执行变更
5. 回归验证
6. 交付复盘

发布/回滚步骤：
- 前置清理：`{{CLEANUP_STEP}}`
- 质量预检：`{{PRE_CHECK_STEP}}`
- 发布执行：`{{PUBLISH_STEP}}`
- 回滚：`{{ROLLBACK_STEP_1}}` -> `{{ROLLBACK_STEP_2}}` -> `{{ROLLBACK_STEP_3}}`
